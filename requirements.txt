# Core dependency: OpenAI client (also used for xAI Grok via OpenAI-compatible API)
openai>=1.55.0

# Optional but useful for future metrics / calibration experiments
numpy>=1.26.0
scikit-learn>=1.5.0
